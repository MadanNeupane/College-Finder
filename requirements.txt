# ===========================================================================
# College Finder — Python Dependencies
# Versions use >= minimum bounds so pip can resolve the latest compatible
# release. Pin to exact versions only if you hit a compatibility issue.
# ===========================================================================

# --- Core Django -----------------------------------------------------------
Django>=3.2
asgiref>=3.7

# --- Database --------------------------------------------------------------
dj-database-url>=2.0          # DATABASE_URL → DATABASES dict
psycopg2-binary>=2.9          # PostgreSQL driver (binary wheel, no build deps)

# --- Static Files ----------------------------------------------------------
whitenoise>=6.0               # Serve compressed static files

# --- Media / Storage -------------------------------------------------------
pillow>=9.0                   # Image processing
django-storages>=1.13         # Cloud storage backends (S3, GCS, etc.)

# --- Authentication --------------------------------------------------------
social-auth-app-django>=5.0   # Django 5.x compatible (4.x used removed force_text)
social-auth-core>=4.3         # Social auth core
python3-openid>=3.2           # OpenID support for social auth
requests-oauthlib>=1.3        # OAuth for requests
oauthlib>=3.1                 # OAuth library
PyJWT>=2.1                    # JSON Web Tokens

# --- Environment -----------------------------------------------------------
python-dotenv>=1.0            # Load .env into os.environ

# --- Django Utilities ------------------------------------------------------
django-taggit>=3.0            # Tagging (3.x+ is Django 4/5 compatible)
django-mathfilters>=1.0       # Math template filters
django-cors-headers>=3.5      # CORS headers

# --- Machine Learning ------------------------------------------------------
scikit-learn>=1.0
numpy>=1.21
joblib>=1.0
scipy>=1.7
threadpoolctl>=3.0

# --- Validation ------------------------------------------------------------
validate_email>=1.3
python-usernames>=0.3
defusedxml>=0.7               # Secure XML parsing (required by social-auth)

# --- Web Servers -----------------------------------------------------------
gunicorn>=20.1                # WSGI server for production
uvicorn>=0.17                 # ASGI server (optional)

# --- Security --------------------------------------------------------------
cryptography>=41.0            # Cryptographic primitives

# --- Monitoring (optional, comment out if not using Sentry) ----------------
sentry-sdk>=1.10

# --- Misc ------------------------------------------------------------------
certifi>=2021.0
urllib3>=1.26
requests>=2.25
six>=1.15
colorama>=0.4
